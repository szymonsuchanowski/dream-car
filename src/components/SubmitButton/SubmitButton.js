import React from 'react';
import StyledSubmitButton from './SubmitButton.styled';

const SubmitButton = ({ value }) => <StyledSubmitButton type="submit" value={value} />;

export default SubmitButton;
