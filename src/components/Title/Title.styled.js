import styled from 'styled-components';

const StyledTitle = styled.header`
    width: 80%;
    margin: 0 auto;
    text-align: center;

    h1 {
        margin: 0;
    }
`;

export default StyledTitle;
